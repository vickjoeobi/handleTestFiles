name: Service Pipeline

on:
  workflow_dispatch:

jobs:
  mock_pipeline:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v2

      - name: Simulate pipeline run
        run: sleep 30

      - name: Simulate report generation
        run: echo "Report generated for ${{ github.repository }}"
